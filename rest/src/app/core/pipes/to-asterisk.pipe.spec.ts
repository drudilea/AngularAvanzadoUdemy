import { ToAsteriskPipe } from './to-asterisk.pipe';

describe('ToAsteriskPipe', () => {
  it('create an instance', () => {
    const pipe = new ToAsteriskPipe();
    expect(pipe).toBeTruthy();
  });
});
